# ext-iv-color-replace

An extension for ImageViewer to perform intelligent color replacement in images.

A simple(ish) algorithm challenge to hand to an LLM for implementation.

Stage 1 will implement a basic color replace based on a source color, a target color, and some threshold value.

Stage 2 will use multi-threading to optimize the approach with parallel processing.


